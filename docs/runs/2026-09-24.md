# Runs of 2026-09-24

One line per run. What each run proved is in the line; the folders that held it are gone, see `README.md` for what is kept.

- 22:05, pass 1 English, all scenarios, launcher died after the game started, request abandoned, no verdict, so
  `exitReason` is `in-progress`. The game played on alone and was stopped at 22:31 when it froze. 26 of 43 scenarios
  passed, 17 failed, 12 of 12 gene recipes passed with durations rising from 8 s to 63 s before the freeze.
  Fifteen failures were one NullReferenceException at Pickle's built-in `I spawn a <def>`, which builds a Pawn
  with no `PawnKindDef` (a vanilla muffalo failed as well), so the suite was at fault and the mod was not: fixed in
  `b903d4f` by a spawn step on `PawnGenerator`. One failure was a 35 s step deadline against a 90 s wait (tunnel):
  fixed. One is not explained yet (the "beast attack now" debug action brought no beast; the step now reports why).
  Kept: `summary.md` and `junit.xml` (`Evidence/2026-09-24-en`).
- 11:23 (2026-09-25 clock), pass 5 ADS2, feature 14 only, English, `exitReason` failed: 4 of 11 passed, 7 failed.
  The four hostile-beast scenarios passed and are the control (no recipe on their races). The six recipe-list
  scenarios failed and so did the load-order one, all for one reason: the staging activates the mods a pass map names
  and then the mod under test, so this mod loaded at position 10 after ADS2 at 9, and ADS2 had copied its category
  lists onto its recipes before the races were added. This confirms the `loadBefore` mechanism and says nothing
  against the patch. The pass map now names this mod first with a `path:` overlay (`wsl-deps.ads2.map`). The launcher
  then failed copying screenshots on a path over 260 characters (fixed on its side).
  Kept: `summary.md` and `junit.xml` (`Evidence/2026-09-24-ads2`).
