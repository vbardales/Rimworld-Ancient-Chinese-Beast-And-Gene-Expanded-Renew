# Runs of 2026-09-25

One line per run, after the fixes of the first day. Folders are kept down to `junit.xml` and `summary.md`.

- 12:00, one scenario, "an ordinary blow does a tenth of its damage to the nian beast", English, `exitReason` failed.
  The spawn step now works (no exception, the scenario reaches its assertion). The assertion failed: a 30-damage cut
  blow on the core dealt 4.909, above the 3.5 ceiling and above the 3.0 a tenth would give. Not explained yet; the blow
  step now writes every factor into the report (`IncomingDamageFactor`, the def multiplier, armour, part health, each
  injury) and feature 09 is resubmitted whole.
- 12:15, feature 12 alone (19 scenarios), English, `exitReason` passed: **19 of 19 green**, each 7 to 13 s, flat. The
  twelve gene recipes, the five clones (a player pawn of the right kind), the archite capsules and the tame mingshe
  that dies with no drought and no error all hold. The dispatcher sampled the machine every 30 s meanwhile: WSL load
  5.8 to 14.9 on 16 processors (the CPU is saturated while the game plays), 3.1 to 4.8 GB free (not a memory
  shortage); its game-memory column was invalid (it measured the xvfb wrapper) and is fixed for the next run. This
  run did not reproduce the freeze of the full pass: the slowdown, 8 s then 25 s then 63 s per scenario, came with
  the length of that run and not with this feature.
- 12:17, one scenario, the sexie's tunnel, exit 3, no Pickle report: the game's own watchdog fired after 120 s in the
  scenario, at the step waiting 90 s of game time for the sexie. The machine was saturated (about six cores taken by the
  WSL game, Windows at 76%), so game time ran slower than real time. The wait is gone: a step now brings the vanilla
  tunnel spawner's deadline to the next tick and waits 20 s for the event.
- 12:20, one scenario, the development actions, `exitReason` failed: "Beast attack now" brought no beast, and the new
  diagnostic says why: `SZ_BeastApproach.CanFireNow` and `SZ_BeastApproachTunnel.CanFireNow` were both false with
  `forced=False` on day 1. A real defect of the mod, small: the action did nothing and said nothing. Fixed in
  `Singleton.BeastApproach(forced)`, the action forces, the scheduler does not; assembly rebuilt (SHA-256
  `BE213D7D...9653A0`).
